services:
  - type: web
    name: openavl
    runtime: python
    plan: free
    buildCommand: pip install ".[web,perf]"
    startCommand: uvicorn openavl.web.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
